import React, { createContext } from 'react';

export const FirstActivityContext = createContext({correct: 0});


/**
export let Activities = {
  activity1: 0,
  activity2: {
    correct: 0,
    incorrect: 0
  }
};

export const FitstActivityContext = createContext(
  // Activities.activity1 // Setting up the deafult context value
);

export const SecondActivityContext = createContext(
  Activities.activity2 // Setting up the deafult context value
);
 */

